<h2>Sign up</h2>

<div class="alert alert-info"><strong>Note:</strong> This is not the signup page for One Month Rails, just the demo app. To sign up for One Month Rails, visit <%= link_to "http://onemonthrails.com", "http://onemonthrails.com" %>.</div>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: { class: 'form-horizontal'}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name  %>
  <%= f.input :email %>
  <%= f.input :password %>

  <div class="form-actions">
          <%= f.submit "Sign up", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>